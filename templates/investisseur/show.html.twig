{% extends 'base.html.twig' %}

{% block title %}Investisseur{% endblock %}

{% block body %}
    <h1>Investisseur</h1>

    <table class="table" id="example" class="table table-striped table-bordered" style="width:100%">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ investisseur.id }}</td>
            </tr>
            <tr>
                <th>Mat</th>
                <td>{{ investisseur.Mat }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ investisseur.Adresse }}</td>
            </tr>
            <tr>
                <th>NomSociete</th>
                <td>{{ investisseur.NomSociete }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('investisseur_index') }}">back to list</a>

    <a href="{{ path('investisseur_edit', {'id': investisseur.id}) }}" class="btn">edit</a>

    {{ include('investisseur/_delete_form.html.twig') }}
{% endblock %}

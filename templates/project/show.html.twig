{% extends 'base.html.twig' %}

{% block title %}Project{% endblock %}

{% block body %}
    <h1>Project</h1>

    <table class="table" id="example" class="table table-striped table-bordered" style="width:100%">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ project.id }}</td>
            </tr>
            <tr>
                <th>CodeP</th>
                <td>{{ project.CodeP }}</td>
            </tr>
            <tr>
                <th>LibelleP</th>
                <td>{{ project.LibelleP }}</td>
            </tr>
            <tr>
                <th>SecteurP</th>
                <td>{{ project.SecteurP }}</td>
            </tr>
            <tr>
                <th>CoutFixe</th>
                <td>{{ project.CoutFixe }}</td>
            </tr>
            <tr>
                <th>CoutVar</th>
                <td>{{ project.CoutVar }}</td>
            </tr>
            <tr>
                <th>DureeP</th>
                <td>{{ project.DureeP ? project.DureeP|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('project_index') }}">back to list</a>

    <a href="{{ path('project_edit', {'id': project.id}) }}">edit</a>

    {{ include('project/_delete_form.html.twig') }}
{% endblock %}
